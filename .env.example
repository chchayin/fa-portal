# FlowAccount MCP Server — local dev config
# Copy to .env for use with `npm run dev`

# Browser visibility during login (always visible — interactive login required)
# FA_HEADLESS is ignored; browser always opens visible for manual login
